create table accounts(
id integer primary key,
username cahr,
nickname varchar);

create table names(
id integer primary key,
account_id integer,
display_name varchar,
changed_date char,
is_first integer);
